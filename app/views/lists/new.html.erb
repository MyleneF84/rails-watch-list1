<%= simple_form_for(@list) do |f| %>
  <%= f.input :name %>
  <%= f.submit "#{ @list.persisted? ? 'Update' : 'Create' } my list", class: 'btn btn-primary' %>
<% end %>

<%= link_to "Back to lists", lists_path %>
